spring:
  cloud:
    config:
      name: data-source
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: cloud-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/